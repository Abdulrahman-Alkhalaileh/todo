import { FcTodoList } from "react-icons/fc";

const Header = () => {
    return (
        <header className="header">
            <FcTodoList size={50} />
            <h1>&nbsp; Todo App</h1>
        </header>
    );
};

export default Header;
